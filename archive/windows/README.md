# Retired Windows launchers

The old root-level `.cmd` shortcut is retained here only for historical
reference. Use the root `setuora.bat` controller for a source checkout.

The active Windows packaging header and scheduled-task runner remain under
`client/windows/` and `scripts/windows/`; they are required by release builds.
